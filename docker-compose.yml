version: '3.8'

services:
  app:
    build: .
    container_name: mes-backend
#    ports:
#      - "8080:8080"
    env_file:
      - .env
networks:
  default:
    name: mes-network
    external: true
